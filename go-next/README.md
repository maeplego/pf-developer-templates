# {{PROJECT}}

Generated by `pf-dev new -t go-next`. Layout matches P04 workspace (`apps/api` + `apps/web`):

- API: P06 catalog + P04 `/health` `/ready` + OIDC userinfo stub + OTel env
- Web: P04 Next.js health/ready routes and OIDC PKCE client stub (`lib/oidc`)

Learning / portfolio sample.

## Test

```powershell
go test ./...
# from apps/api

cd apps/web
npm test
```

## Run

```powershell
$env:DEV_AUTH="true"
go run ./apps/api/cmd/server
# other terminal
cd apps/web; npm install; npm run dev
```
